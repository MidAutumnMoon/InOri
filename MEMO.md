# MEMO

## imgo

### SharedOpts

- `non_recursive` flag (`-R`, `--non-recursive`): Disables recursion into subdirectories.
  - When set without manual selection: Only images from the workspace/current directory are processed.
  - When set with manual selection: Directories in the manual selection are ignored (files are still processed).
